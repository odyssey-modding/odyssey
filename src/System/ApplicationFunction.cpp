#include "ApplicationFunction.h"
#include <framework/seadGameFrameworkNx.h>

void ApplicationFunction::initialize(s32 argc, char** argv) {
    // TODO: initialize is non-static in sead headers, change it to be non-static later
    // sead::Framework::InitializeArg arg;
    // arg.heap_size = 0xbfc00000;
    // sead::GameFrameworkNx::initialize(arg);
}
